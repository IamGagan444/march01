export default function AuthLayouts({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
      <>
        {children}
        <h2>this is authentication layouts</h2>
      </>
    );
  }
  